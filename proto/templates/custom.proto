syntax = "proto3";

package templates;

import "templates/options.proto";

option go_package = "pkg/templates";

message Custom {
  bool enabled = 1;
  string name = 2;
  repeated TemplateOptions depends_on = 3;
  string repo_url = 4;
}
